# Search (탐색 알고리즘)

# 🔍 탐색 알고리즘 (Search Algorithms)

> 데이터 집합 내에서 특정 값을 찾기 위한 알고리즘.  
> 자료 구조의 형태나 데이터의 정렬 여부에 따라 다양한 방식이 존재한다.  
> 효율적인 탐색은 프로그램의 성능 향상에 핵심적인 역할을 한다.

---

## 📘 1. 탐색 알고리즘이란?

탐색(Search)이란 **주어진 자료 구조 내에서 특정 값(키)** 을 찾는 과정을 의미한다.  
탐색 알고리즘은 다음과 같은 요소에 따라 달라진다.

- 자료의 **정렬 여부**
- 사용하는 **자료구조 (배열, 연결리스트, 트리, 그래프 등)**
- **탐색 목적** (단순 존재 여부 확인 / 경로 탐색 / 최단 거리 등)

---

## 🧩 2. 탐색 알고리즘의 종류

| 탐색 방식 | 설명 | 적용 자료구조 |
|------------|-------------------------------|-----------------------------|
| **선형 탐색 (Linear Search)** | 처음부터 끝까지 하나씩 비교하며 찾음 | 배열, 연결리스트 |
| **이진 탐색 (Binary Search)** | 정렬된 배열을 반으로 나누며 탐색 | 정렬된 배열, 이진 탐색 트리 |
| **해시 탐색 (Hash Search)** | 해시 함수를 통해 인덱스로 직접 접근 | 해시 테이블 |
| **DFS (Depth-First Search)** | 깊은 노드부터 탐색하는 방식 | 그래프, 트리 |
| **BFS (Breadth-First Search)** | 가까운 노드부터 탐색하는 방식 | 그래프 |
| **트리 탐색 (Tree Search)** | 이진 탐색 트리나 균형 트리에서 탐색 | BST, AVL, B-Tree 등 |

---

## ⚙️ 3. 주요 탐색 알고리즘 비교

| 알고리즘 | 설명 | 시간 복잡도 |
|-----------|--------------------------------|----------------|
| **선형 탐색** | 모든 원소를 순차적으로 확인 | O(n) |
| **이진 탐색** | 탐색 범위를 절반으로 줄이며 탐색 | O(log n) |
| **해시 탐색** | 해시값으로 직접 접근 | 평균 O(1), 최악 O(n) |
| **DFS / BFS** | 그래프 탐색 | O(V + E) |
| **트리 탐색 (균형 트리)** | 트리 높이에 비례한 탐색 | O(log n) |

---

## 🌲 4. DFS vs BFS 비교

| 항목 | DFS (깊이 우선 탐색) | BFS (너비 우선 탐색) |
|-------|-----------------------------|-----------------------------|
| **탐색 순서** | 한 경로를 끝까지 탐색 후 돌아옴 | 가까운 노드부터 차례로 탐색 |
| **구현 방식** | 재귀 함수 or Stack | Queue |
| **사용 예시** | 미로 탐색, 백트래킹 | 최단 거리, 레벨 탐색 |
| **메모리 사용** | 상대적으로 적음 | 경우에 따라 DFS보다 많음 |
| **적합한 문제** | 모든 경로 탐색 | 최단 경로 탐색 |

> ✅ **팁**
> - "깊게 파고들며 탐색" → DFS  
> - "가까운 것부터 넓게 탐색" → BFS  
> - 최단 거리 문제에서는 BFS가 적합

---

## ⏱️ 5. 시간 복잡도 정리

| 알고리즘 | 최선 | 평균 | 최악 |
|-----------|--------|--------|--------|
| **선형 탐색** | O(1) | O(n) | O(n) |
| **이진 탐색** | O(1) | O(log n) | O(log n) |
| **해시 탐색** | O(1) | O(1) | O(n) |
| **DFS / BFS** | O(V) | O(V + E) | O(V + E) |
| **트리 탐색** | O(log n) | O(log n) | O(log n) |

---

## 💡 6. 탐색 알고리즘 활용 사례

- **검색 엔진** → 키워드 색인, 해시 탐색  
- **네트워크 라우팅** → BFS, DFS 기반 경로 탐색  
- **게임 개발** → 맵 탐색, NPC 경로 탐색  
- **AI / 문제 해결** → 상태공간 탐색 (DFS, BFS)  
- **데이터베이스** → B+Tree, 인덱스 기반 이진 탐색  

---

## 🧠 7. 알고리즘 선택 시 고려사항

- 자료 구조의 **특성** (배열, 그래프, 트리 등)
- 데이터의 **정렬 여부**
- **메모리 사용량**
- 실제 데이터의 **균형도 / 분포도**
- **탐색 목적** (존재 여부, 최단 거리, 모든 경로 등)

---

## 🏁 8. 결론

탐색 알고리즘은 모든 시스템의 **기본적인 연산의 핵심**이다.  
효율적인 탐색을 위해서는 단순히 복잡도를 아는 것뿐 아니라,  
**자료구조의 성격**, **문제의 형태**, **실제 데이터의 구조**를 함께 고려해야 한다.

> “탐색은 모든 알고리즘의 시작이다.”  
> — 효율적인 탐색은 효율적인 프로그램의 핵심이다.
>
## 출처
- https://itpe.jackerlab.com/entry/%ED%83%90%EC%83%89-%EC%95%8C%EA%B3%A0%EB%A6%AC%EC%A6%98Search-Algorithms
